include_guard()

if("@SFML_OS_ANDROID@" OR "@SFML_OS_IOS@")
    find_dependency(ZLIB)
endif()

if("@SFML_OS_IOS@")
    find_dependency(BZip2)
endif()

find_dependency(Freetype)

if(SFML_FOUND)
    include("${CMAKE_CURRENT_LIST_DIR}/SFMLModuleTargetsGraphics.cmake")
endif()
